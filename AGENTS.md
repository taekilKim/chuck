# Chuck — Agent / Codex Instructions

이 저장소에서 작업하는 AI 에이전트는 코드 작업 전에 `docs/PROJECT_CONTEXT.md`와 `docs/DECISIONS.md`를 먼저 읽는다.

## 현재 확정된 프로젝트 정체성

- 현재 서비스명은 **Chuck**이다.
- `척척박사`는 과거 가제이며 새 UI, 카피, 코드의 브랜드명으로 되돌리지 않는다.
- Vercel 프로젝트의 내부 이름 `cheokcheok-doctor`는 변경하지 않아도 된다. 서비스 브랜드와 배포 프로젝트 식별자는 별개다.
- 제품은 앱보다 **웹 MVP**로 먼저 검증한다.
- 핵심 명제는 **“보고 싶은 콘텐츠와 알고 싶은 콘텐츠는 다르다.”**
- 단순 요약기나 `작품명 → GPT → 긴 답변`을 만들지 않는다. 목표는 작품을 이해시키는 구조화된 인터페이스다.

## MVP에서 검증할 것

제1가설은 한 작품의 구조화된 이해 경험이 충분히 가치 있어서 사용자가 다른 작품도 Chuck에서 다시 찾는지 여부다.

우선순위가 가장 높은 행동 지표는 `첫 작품 → 두 번째 작품 검색 전환율`이다.

## MVP 핵심 경험

1. 작품 검색
2. 정확한 작품 식별 / 동명이작 처리
3. 1분 핵심 이해
4. 스포일러 포함 전체 줄거리
5. 등장인물 관계 / 핵심 사건
6. 복선·반전·장르/서술/연출 장치
7. 결말과 주요 해석
8. 유명 장면·문화적 대화 포인트
9. 작품 맥락 기반 후속 질문
10. 다른 작품 검색으로 자연스럽게 이어지는 탐색
11. 자료 충분성 / Coverage 상태
12. 검색·열람·후속질문·두 번째 검색 등의 기본 행동 로그

## MVP에서 우선 제외

- 친구 초대
- 문화 궁합
- 문화력 점수 / 랭킹
- 대규모 소셜 피드
- 복잡한 취향 추천
- 세상의 모든 콘텐츠를 미리 구축하는 Full Content DB

## 최상위 신뢰 규칙

**근거가 없으면 생성하지 않는다.**

공개 자료나 신뢰할 수 있는 근거로 확인되지 않는 사건, 인물, 결말, 복선 등을 AI가 그럴듯하게 완성하지 않는다. 사실 / 강한 추론 / 해석 / 확인 불가를 구분한다. 자료가 부족하면 불완전한 결과를 정상 상태로 인정하고 Coverage Level을 표시한다.

## 데이터 전략

초기에는 `얇은 작품 메타데이터 + 외부 자료 검색 + AI 분석/구조화 + 생성 결과 캐시`를 기본으로 한다.

- Popular: 사전 생성 및 검수
- Long tail: 최초 요청 시 생성 후 캐시
- Dark Catalog: 자료 부족 상태를 명시하고 요청을 축적. MVP 이후 요청 기반 조사/기여/검수 검토

## 저작권 / 콘텐츠 원칙

- 소설 원문 장문 재현 금지
- 영화·드라마 원본 영상/캡처/장면 이미지 사용을 기본적으로 피한다.
- 결과물은 사실 재구성, 관계 설명, 분석, 비평, 해설 등 독자적 편집이 개입되어야 한다.
- “AI 해석을 거쳤다”는 이유만으로 저작권 리스크가 사라진다고 가정하지 않는다.
- 원작 표현의 실질적 복제와 시장 대체 위험을 최소화한다.

## 제품 판단 방식

기존 아이디어를 모두 확정된 사실처럼 취급하지 않는다. `확정 원칙 / 현재 가설 / 장기 아이디어`를 구분한다. 범용 GPT 대비 차별성, 저작권, 희귀 작품 공급 비용, 정확도, 반복 사용 동기를 계속 비판적으로 검토한다.

새로운 기능을 추가할 때는 먼저 **“이 기능이 제1가설 검증에 필요한가?”**를 묻는다. 필요하지 않으면 MVP 이후로 미룬다.
